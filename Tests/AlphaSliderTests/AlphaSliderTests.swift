import XCTest
@testable import AlphaSlider

final class AlphaSliderTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AlphaSlider().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
